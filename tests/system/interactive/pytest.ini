# Skip interactive tests until pexpect framework is fixed
# These tests have process management issues that cause errors
# See README_PEXPECT_ISSUE.md for details

[pytest]
markers =
    skip_interactive: skip all interactive tests due to pexpect issues